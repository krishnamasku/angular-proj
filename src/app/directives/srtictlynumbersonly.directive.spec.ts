import { SrtictlynumbersonlyDirective } from './srtictlynumbersonly.directive';

describe('SrtictlynumbersonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new SrtictlynumbersonlyDirective();
    expect(directive).toBeTruthy();
  });
});
